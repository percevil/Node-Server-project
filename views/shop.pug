extends layouts/main-layout.pug

block styles
      link(rel="stylesheet", href="/css/forms.css")
      link(rel="stylesheet", href="/css/product.css")


block content 
    main    
            if prods.length > 0    
                //-  ☝︎☝︎ Adding an if else statement will provide a check if you have products( in the key: prods!) run the code below. Or if you have none, run the else statement with the h1 text onto the screen.
                -//
                 div.grid 
                     each product in prods
                        //- ↑↑ This line uses the keyword 'each' to loop through into a value where you want to store the iteration (product) and then in the array that you want to loop (prods). Prods referring to line #13 in /routes/shop.js key passing to our view.//
                        article.card.product-item
                            header.card__header
                                h1.product__title #{product.title}
                                    //- #{product.title} pushes  the book title dynamically through the title key from line #18 in routes/admin.js
                            div.card__image
                                img(src="https://cdn.pixabay.com/photo/2016/03/31/20/51/book-1296045_960_720.png", alt="A Book")    
                            div.card__content
                                h2.product__price $19.99
                                p.product__description A very interesting book about so many even more interesting things!
                            div.card__actions
                            button.btn Add to Cart 
            else 
                h1 No Books Found   
                 //- If no products, outputs this to the screen.




